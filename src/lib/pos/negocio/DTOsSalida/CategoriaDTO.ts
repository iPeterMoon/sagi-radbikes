/** Información simplificada de la categoría de un producto. */
export interface CategoriaDTO {
  /** Identificador primario de la categoría */
  idCategoria: string;
  /** Nombre descriptivo de la categoría */
  nombre: string;
}